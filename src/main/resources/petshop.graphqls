type Query {
    pets: [Pet]
    cars: [Car]
}

type Pet {
    id: Int
    type: Animal
    name: String
    age: Int
}

enum Animal {
    DOG
    CAT
    BADGER
    MAMMOTH
}

type Car {
    id: Int
    make: String
    model: String
    year: Int
}